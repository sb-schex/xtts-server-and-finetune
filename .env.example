# Authentication
AUTH_USER=admin
AUTH_PASSWORD=xtts2024
JWT_SECRET=your-secret-key-change-in-production

# Backend API
# For local GPU server:
BACKEND_URL=http://localhost:8000
# For Modal (cloud):
# MODAL_API_URL=https://your-modal-app.modal.run

# Data directories (for local development)
UPLOAD_DIR=/tmp/xtts-uploads
DATASETS_DIR=/tmp/xtts-datasets
OUTPUT_DIR=/tmp/xtts-outputs
SPEAKERS_DIR=/tmp/xtts-speakers
